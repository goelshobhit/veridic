/*
 * HoverableCard Messages
 *
 * This contains all the text for the HoverableCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HoverableCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HoverableCard component!',
  },
});
