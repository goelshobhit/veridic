/*
 * AnimeCard Messages
 *
 * This contains all the text for the AnimeCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AnimeCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AnimeCard component!',
  },
});
